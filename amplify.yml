version: 1
applications:
  - appRoot: amplifymonorepo/reactapp
    frontend:
      phases:
        preBuild:
          commands:
            - echo "============================================"
            - echo "From build local project"
            - echo "============================================"
            - npm ci
        build:
          commands:
            - npm run build
      artifacts:
        files:
             - '**/*'
        baseDirectory: build
      cache:
        paths:
             - node_modules/**/*
  - appRoot: /docs
    frontend:
      phases:
        build:
          commands:
            - echo "Nothing to build"
      artifacts:
        files:
             - '**/*'
        baseDirectory: amplifymonorepo/docs
      cache:
        paths: []



