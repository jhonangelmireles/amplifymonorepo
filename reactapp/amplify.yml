version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - echo "from root project"
            - npm ci
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: build
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    appRoot: reactapp
