version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - echo "============================================"
            - echo "From build root project"
            - echo "============================================"
            - npm ci
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: build
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    appRoot: reactapp
